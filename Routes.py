#Routes.py

class Routes(object):
	#This class will be responsible for all the functionality surrounding the routes
	#This class will also need to interface with the database to save preferences etc.

	def __init__(self):
		#this is the constructor, initialize whatever variables are needed here

	def addRoute(inR):
		#add a route to the database

	def removeRoute(inR):
		#remove the route from the database

	def modifyRoute(inR):
		#where preferences come into play also shortest/simplest route

	def getRoute(inR):
		#receive a request to return a new route
